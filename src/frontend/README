Put here your GUI code here created with your favorite front-end technologies. The backend is configured to look for
frontend code in the `gui` directory of the root directory. So the code put here shall be configured to produce output
to the `gui` directory (e.g. using webpack, gulp or equivalent). Or forget all this and just put your frontend code
directly in the `gui` directory.


将您使用喜欢的前端技术创建的 GUI 代码放在这里。后端已配置为在根目录中的 gui 文件夹中查找前端代码。因此，
放在这里的代码应配置为将输出生成到 gui 文件夹中（例如，使用 webpack、gulp 等工具）。
或者，您也可以忽略这些步骤，直接将前端代码放入 gui 文件夹。




安装依赖
npm install --registry=http://registry.npmmirror.com
 npm install element-plus --save --registry=http://registry.npmmirror.com